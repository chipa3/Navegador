PARAMETROS EN FUNCION;
string usuario, Form formularioPadre

FUERDA DE LA FUNCION;
        string UsuarioAplicacion;
        static Form FormularioPadre;

DENTRO DE LA FUNCION;
UsuarioAplicacion = usuario;
            navegador1.Usuario = UsuarioAplicacion;
            FormularioPadre = formularioPadre;



//Navegador:

 List<string> CamposTabla = new List<string>();
            List<Control> lista = new List<Control>();
            //navegador1.aplicacion = numero de la aplicacion que se abre;
          //  navegador1.tbl = "nombre de la tabla";
           // navegador1.campoEstado = "nombre del estado de la tabla";
            navegador1.MDIformulario = FormularioPadre;
            foreach (Control C in this.Controls)
            {
                if ((C.Tag != null) && (!C.Tag.ToString().Equals("")))
                {
                    if (C is TextBox)
                    {
                        lista.Add(C);

                    }
                    else if (C is ComboBox)
                    {
                        lista.Add(C);

                    }
                    else if (C is DateTimePicker)
                    {
                        lista.Add(C);
                    }
                }
            }
            navegador1.control = lista;
            navegador1.formulario = this;
           // navegador1.DatosActualizar = dataGridView que se agrega al formulario;
            navegador1.procActualizarData();
            navegador1.procCargar();
          //  navegador1.ayudaRuta = "carpeta/archivo.chm del usuario";
          //  navegador1.ruta = "html que se abre";